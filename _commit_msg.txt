feat: 无行维度输出改为按位置1:1对应 + 横向展开 (v2.10.6)

- 无行维度时，值字段与聚合方式改为按位置 1:1 对应（与有行维度一致）
  - 单值多聚合时保持全组合（兼容有行维度行为）
- 无行维度结果改为横向展开（一行多列），替代原来的纵向"指标|值"两列表
- 新增 _get_agg_label 辅助函数，统一聚合方式的中文标签生成
- 防护用例新增无行维度测试任务，全部 32+9+5=46 项通过
